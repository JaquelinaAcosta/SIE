{% extends "AppBundle::layout.html.twig" %}

{% block content %}
    
    <style>
        input[type=text]{
            margin-bottom: 10px;
            padding:10px;            
         }
         input[type=submit]{
            margin-bottom: 10px;
            padding:10px;            
         }

    </style>
    
    
    <div class="col-ls-12 col-md-offset-1">
        <div class="panel panel-primary ">
            <div class="title panel-heading">Expediente -> Movimiento</div>
            <div class="panel panel-body">
               <h3 class="text-default">Expediente Número: {{ expediente.nroExpediente }}</h3>
            
           
            <hr/>
            
            {{form_start(form,{'action':'','method':'POST'})}}
                      
                 <div class="col-lg-2">
                        <label class="text-default">FECHA</label>
                        {{ form_row(form.fecha, { 'id': 'date1' })}} 
                        {{ form_errors(form.fecha) }}
                        {{ form_widget(form.fecha) }}
                    </div>  
            
            {{form_end(form)}}

            </div>
        </div>
    </div>          
    
{% endblock %}