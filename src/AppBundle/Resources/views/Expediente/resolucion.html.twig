
{% extends "AppBundle::layout.html.twig" %}

{% block content %}
    
    <style>
        input[type=text]{
            margin-bottom: 10px;
            padding:10px;            
         }
         input[type=submit]{
            margin-bottom: 10px;
            padding:10px;            
         }

    </style>
    
    {{ dump(usuario) }}
    
    <div class="formu col-lg-4 col-md-offset-1">
        <div class="border border-primary">
            <h4 class="text-center">Expediente->Resolución</h4>
            <hr/>
            {{form_start(form,{'action':'','method':'POST'})}}
            
             <label class="text-default">FECHA DE RESOLUCION</label>
                        {{ form_row(form.fechaResolucion, { 'id': 'date1' })}} 
                        {{ form_errors(form.fechaResolucion) }}
                        {{ form_widget(form.fechaResolucion) }}
            
            
            {{form_end(form)}}
        </div>
    </div>
{% endblock %}